# HTTP-клиент для Extract-шага: фиксируем версию для стабильного поведения API-вызовов между окружениями.
requests==2.32.3
# Табличная трансформация данных в Transform-шаге: фиксируем версию для предсказуемой обработки типов.
pandas==2.2.2
# Драйвер PostgreSQL для SQLAlchemy в Load-шаге: фиксируем версию для совместимости подключения.
psycopg2-binary==2.9.10
# ORM/DB toolkit для транзакционной загрузки и вызова процедур: фиксируем версию для повторяемых запусков.
sqlalchemy==2.0.36
# Пакет для load_dotenv: устраняем риск расхождений импорта и окружений.
python-dotenv==1.0.1